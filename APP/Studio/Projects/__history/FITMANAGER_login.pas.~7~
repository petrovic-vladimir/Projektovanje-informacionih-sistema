unit FITMANAGER_login;

interface

uses
  System.SysUtils, System.Types, System.UITypes, System.Classes, System.Variants,
  FMX.Types, FMX.Controls, FMX.Forms, FMX.Graphics, FMX.Dialogs, FMX.Layouts,
  FMX.ExtCtrls, FMX.Objects, FMX.Colors, FMX.Controls.Presentation, FMX.StdCtrls,
  FMX.Ani, FMX.Edit,FITMANAGER_register,FITMANAGER_dashboard,dmMain,DB;

type
  TFrmLogin = class(TForm)
    LoginTemplate: TImage;
    loginBtn: TButton;
    FloatAnimation1: TFloatAnimation;
    Email_input: TEdit;
    Pass_input: TEdit;
    Label1: TLabel;
    Create_account: TButton;
    Label2: TLabel;
    procedure Create_accountClick(Sender: TObject);
    procedure loginBtnClick(Sender: TObject);
  private
    { Private declarations }
  public
    { Public declarations }
  end;

var
  FrmLogin: TFrmLogin;

implementation


  {$R *.fmx}
  {$R *.XLgXhdpiTb.fmx ANDROID}
  {$R *.iPhone4in.fmx IOS}
  {$R *.GGlass.fmx ANDROID}
  {$R *.Moto360.fmx ANDROID}
  {$R *.SSW3.fmx ANDROID}
  {$R *.iPhone55in.fmx IOS}
  {$R *.iPhone.fmx IOS}
  {$R *.NmXhdpiPh.fmx ANDROID}
  {$R *.SmXhdpiPh.fmx ANDROID}
  {$R *.LgXhdpiPh.fmx ANDROID}


  procedure TFrmLogin.loginBtnClick(Sender: TObject);
    var users : string;
    begin
      
      frmDashboard.Show;
      dmDB.Query_getAllUsers.Connection;
      dmDB.DbConnect.Connected := True;

      
      
    end;

  procedure TFrmLogin.Create_accountClick(Sender: TObject);
    begin
      if frmRegister = nil then
      frmRegister := TfrmRegister.Create(Application);

      frmRegister.Show;
      Self.Hide;
    end;

end.
